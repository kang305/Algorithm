图的存储：《数据结构》涉及5种存图方式：邻接矩阵，邻接表，十字链表，多重邻接表，边集数组。
其中：邻接表，十字链表，多重邻接表 是基于链表实现的，涉及到指针指向，代码操作复杂，且容易出现空指针，野指针的bug。
其中，刷题时常用的是邻接矩阵和邻接表变形（链式前向星），但是因为上述原因，需要对邻接表的代码实现进行改进。（理论（存图的逻辑）不变）
邻接矩阵和边集数组本身就是基于数组的，按照《数据结构》课上讲解的实现即可。


带权图的邻接矩阵。用静态链表实现：即用边集数组和邻接表的逻辑相结合，可以快速访问一个顶点的所有邻接点----链式前向星
边集数组：edge[ ]，edge[i]表示第i条边，存储边的信息：to,w,next,其中next不再是指针，而是同起点的下一条边的编号（下标）。
头结点数组：head[ ]，head[x]存以x为起点的第一条边的下标(下标指:在edge[]中的下标)。初始时head[]数组全部初始化为-1，即认为没有边。
head[x]=2;   edge[2]是x的一条出边



稠密图：邻接矩阵。>nlogn
稀疏图：邻接表-->二维vector/链式前向星





